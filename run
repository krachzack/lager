#!/bin/bash

nodemon --exec "dub -- examples/config.yaml out.json" \
        --watch "pils" \
        --watch "source" \
        --watch "examples" \
        --watch "dub.sdl" \
        --watch "run"
