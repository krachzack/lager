#!/bin/bash

nodemon --exec "sh -c 'dub test -- examples/config.yaml ~/out.json; :'" \
        --ext "d,sdl,yaml,json" \
        --watch "pils" \
        --watch "source" \
        --watch "examples" \
        --watch "dub.sdl" \
        --watch "run"
